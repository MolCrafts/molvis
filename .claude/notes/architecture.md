# Architecture Blueprint

> **Stub.** Run `/mol:map` to populate this blueprint — modules, public surfaces,
> layer roles, and dependency rules — from the current source. The `librarian`
> agent reads this file during `/mol:spec` to place new work correctly.

Until then, the load-bearing rules live in `CLAUDE.md` § **Invariants**, and the
detailed pre-rebuild architecture reference is recoverable from git history.
